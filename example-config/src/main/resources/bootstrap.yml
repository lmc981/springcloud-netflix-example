# bootstrap.yml
spring:
  application:
    name: example-config
  profiles:
    active: native
eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}/eureka/
